extends layout

block variables
  - var title= "GameLibrary Home"
  
block content
  h1= title
  p Welcome to #[em GameLibrary].

  h1 Records

  if error
    p Error getting dynamic content.
  else
    p The library has the following record counts:

    ul
      li #[strong Games:] !{data.game_count}
      li #[strong Game Copies:] !{data.game_instance_count}
      li #[strong Game Copies available:] !{data.game_instance_available_count}
      li #[strong Developers:] !{data.developer_count}
      li #[strong Genres:] !{data.genre_count}